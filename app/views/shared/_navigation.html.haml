.navbar.navbar-inverse.navbar-fixed-top
  .navbar-inner
    .container-fluid
      %button.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse", type: "button"}
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar   
      - if current_user 
        = link_to link_text("AyusClaim", ''), claims_path, :class => 'brand'
      .nav-collapse.collapse
        %ul.nav.navbar-nav
    
          - if current_user
            %ul.nav
              %li= link_to "Claims" , claims_path, :class => "#{ ('disabled' if request.fullpath == claims_path) }"
              %li= link_to "Checklist" , checklists_path, :class => "#{ ('disabled' if request.fullpath == checklists_path) }"
              %li= link_to "Statistics" , claim_stats_path, :class => "#{ ('disabled' if request.fullpath == checklists_path) }"
      .pull-right.navbar-text
        %ul.nav
          %li= link_to "LOG OUT".html_safe, destroy_user_session_path,:method => :delete,:class =>'navbar-brand'
      .clearfix
%br